package com.mfs.pricingprofile.repo;

import com.mfs.pricingprofile.model.TblTransChargesChannel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblTransChargesChannelRepo extends JpaRepository<TblTransChargesChannel, Long> {
}
