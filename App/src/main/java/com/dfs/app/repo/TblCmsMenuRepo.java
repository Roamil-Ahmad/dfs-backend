package com.dfs.app.repo;

import com.dfs.app.model.TblCmsMenu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblCmsMenuRepo extends JpaRepository<TblCmsMenu, Long> {
}
