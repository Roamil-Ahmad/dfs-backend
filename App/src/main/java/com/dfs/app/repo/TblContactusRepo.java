package com.dfs.app.repo;

import com.dfs.app.model.TblContactus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblContactusRepo extends JpaRepository<TblContactus,Long> {
}
