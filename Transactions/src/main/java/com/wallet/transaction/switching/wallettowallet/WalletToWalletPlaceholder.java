package com.wallet.transaction.switching.wallettowallet;

/**
 * Reserved for the Wallet-to-Wallet flow.
 *
 * Nothing is implemented here yet - the package exists so the flow can be added beside IBFT without
 * moving anything. When it is built it will follow the same shape as
 * {@code com.wallet.transaction.switching.ibft} and call {@code PKG_MW.WALLET_TO_WALLET}.
 */
public final class WalletToWalletPlaceholder {

    private WalletToWalletPlaceholder() {
    }
}
