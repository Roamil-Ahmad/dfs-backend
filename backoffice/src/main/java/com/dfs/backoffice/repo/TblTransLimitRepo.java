package com.dfs.backoffice.repo;

import com.dfs.backoffice.model.TblTransLimit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TblTransLimitRepo extends JpaRepository<TblTransLimit,Long> {
}
